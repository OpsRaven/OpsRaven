This folder is used for powershell scripts that can be used 
with Corsair iCUE to assign hotkeys to execute the scripts.
You will need PS2EXE (powershell to exe).

Example: instead of physically or memorizing an application's
mute button. You can set a hotkey to mute the mic at the system
level.